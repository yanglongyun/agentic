#!/bin/sh
set -eu
agent_root=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
if [ -x "$agent_root/runtime/bin/node" ]; then
  exec "$agent_root/runtime/bin/node" "$agent_root/server/scripts/cli.js" "$@"
fi
exec node "$agent_root/server/scripts/cli.js" "$@"

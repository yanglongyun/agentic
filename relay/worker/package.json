{
  "name": "agentic-relay",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run",
    "test": "node --test --test-timeout=30000 integration.test.js"
  },
  "devDependencies": {
    "wrangler": "4.132.0"
  }
}

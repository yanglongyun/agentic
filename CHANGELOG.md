# 更新记录

## 0.3.0

- 用异步 `agent` 工具替换 `delegate`：派发后立即返回 agent ID，子任务完成后自动回复到所属对话。
- 子任务独立保存消息、压缩记录和状态，嵌套在父会话的 `agents/` 下，不出现在 `/resume` 列表。
- 等待子任务时释放执行名额，支持并发限制、嵌套深度限制、超时与级联取消。
- 结果交付持久化，恢复时依据交付记录避免重复处理；切换会话后，结果仍回到原会话。
- 子任务返回时保留终端中正在输入的草稿；当前回复结束后再处理后台结果。
- 子任务结果提示词通过 `agent_result_prefix` 配置。

### 升级变化

- 工具名 `delegate` 改为 `agent`，调用结果改为异步任务标识。
- 保留现有 0.2.0 配置与会话，不迁移旧数据格式。

## 0.2.0

- `agent` 同时启动终端聊天和 HTTP API，默认监听 `127.0.0.1:9528`；退出时关闭服务并取消未完成任务。
- API 支持异步任务、SSE、取消和独立子任务。访问令牌自动生成并保存到 `config.json` 的 `api.token`。
- 发送第一条消息时才创建会话；空会话不再显示在历史列表。
- `/resume` 支持分页选择、恢复历史对话。默认回放最近 20 条，读取与显示量均有限制。
- Esc / Ctrl+C 可中断当前回复，终端退出后恢复键盘模式。
- 简化帮助和启动信息，优化用户消息背景、会话列表颜色与翻页提示。
- 按压缩节点读取活动上下文，SSE 使用事件通知，减少长会话的重复读取与轮询。

### 升级变化

- 移除 `agent serve`，统一使用 `agent`；`agent resume` 同样启动 API。
- 移除手动压缩、`/messages`、reset 和单次参数提问；上下文自动压缩。
- 运行数据统一为 `config.json`、`state.json`、`sessions/`；会话包含 `session.json`、`messages.jsonl` 和 `compactions.jsonl`。
- 不迁移旧数据格式。遇到旧格式提示时，使用新的 `AGENT_HOME` 并重新配置模型。
- API 的地址、令牌与执行限制位于配置的 `api` 中；端口占用时明确报错。

发布包：Linux、macOS、Windows，各提供 amd64 / arm64 架构，以及 SHA-256 校验文件。
